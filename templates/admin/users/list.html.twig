{% extends "base.html.twig" %}
{% block body %}
<div class="d-flex">
{% include "admin/admin.html.twig" %}
<div class=" admin-action">
<div class="flex-center-col">
<div class="flex-center-col table-container">

  <h2 class="text-center p-4">Liste d'utilisaturs</h2>
    
  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th> Roles </th>
        <th>Email</th>
        <th>Telephone</th>
        <th>Changer Role</th>
        <th></th>
      </tr>
  </thead>
  <tbody>
  {% for user in users %}
    <tr>
      <th scope="row"><img  src="{{user.image ? vich_uploader_asset(user) : '../uploads/profil.jpeg'}}" width="45px" height="50px"/></th>
      <td>{{user.name}} {{user.lastname}}</td>
      <td> 
        {% for role in user.roles %}
        {% if role =='ROLE_ADMIN' %}

        <p>Administrateur</p>
        {% elseif role =='ROLE_THERAPIST'%}
        <p>Thérapeute</p>
        {% elseif role =='ROLE_USER'%}
        <p>Utilisateur</p>
        {% endif %}

        {% endfor %}
      </td>
      <td>{{user.email}}</td>
      <td>{{user.phone}}</td>
      <td><a href="{{path( 'admin_users_updateUser', {id:user.id})}}">Modifier</a></td>
    </tr>
    {% endfor %}
    
  </tbody>
</table>
        
  
</div>
{% endblock %}