{% extends "base.html.twig" %}
{% block body %}
<div class="d-flex">
{% include "admin/admin.html.twig" %}
<div class=" admin-action">
<div class="flex-center-col">
<div class="flex-center-col table-container">
    <h3 class="text-center p-4">ArtEvents Approuved  <i class="fa-solid fa-check"></i></h3>
    <table>
        <thead>
            <tr>
                <th>ArtEvent</th>
                <th>Prix</th>
                <th>Intervenant</th>
                <th>Action</th>
            </tr>
        </thead> 
        <tbody>
            {% for event in events %}
            <tr>
                <td>
                <a class="detail-link" href="{{path( 'app_event_showDetail', {id:event.id})}}">
                    <div class="d-flex" >
                    <img class="basket-image"src={{event.image ? vich_uploader_asset(event) : asset('images/placeholder.jpg')}} width="120" height="100"> 
                    <div class="pl-2">  
                        <p>{{event.name|u.truncate(20, '...', false)}}</p>
                        <p>{{event.date|date}}</p>
                        </div>
                    </div>
                    </a>
                </td>
                <td>{{event.price}}€</td>
                <td>
                    <p></p>
                   
                </td>
                <td>
                    <div>
                        <a class="btn-admin btn-up"href="{{path('admin_event_update', {id:event.id})}}">Modifier <i class="fa-solid fa-pencil"></i></a>
                    </div>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
</div>
{% endblock %}