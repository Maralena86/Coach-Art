<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" type="text/css" href="/style.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" integrity="sha512-xh6O/CkQoPOWDdYTDqeRdPCVd1SpvCA9XXcUnZS2FmJNp1coAFzvtCN9BmamE+4aHK8yyUHUSCcJHgXloTyT2A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Mali:wght@300;400&family=Nanum+Gothic:wght@400;700;800&display=swap" rel="stylesheet">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
       {% block stylesheets %}
         
        {% endblock %}
        
    </head>
    <body>
    
        <header class="d-flex">
        
        
            <nav class="navbar navbar-expand-lg ">
            
                <a class="navbar-brand text-light" id="logo" href="{{path('app_home_display')}}">Coach-Art</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav d-flex ">
                    <li class="nav-item active ">
                        <a class="nav-link text-light" href="#">A propos de nous<span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="nav-link text-light " href="{{path('app_event_create')}}">Créer ArtEvent</a>
                        {% endif %}
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light  p-2 " href="{{path('app_events_list')}}">ArtEvents</a>
                    </li>
                    <li class="nav-item dropdown  ">
                        <a class="nav-link text-light dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa-solid fa-user"></i>
                        </a>
                        
               
                        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdownMenuLink">
                             {% if app.user %}
                            {# <p>Bienvenue {{app.user.email}} </p> #}
                            {% if is_granted('ROLE_USER') %}
                            {# <a href="{{path('app_security_myProfile')}}">Changer le profil</a>       #}
                            <a class="dropdown-item text-light" href="{{path('app_basket_display')}}">Mon panier</a>  
                            <a class="dropdown-item" href="{{path('app_security_showProfil', {id:app.user.id})}}">Profil</a>         
                            {% endif %}              
                            <a class="dropdown-item" href="{{path('app_logout')}}">Se déconnecter</a>
                            {% else %}
                            <a class="dropdown-item" href="{{path('app_login')}}">Se connecter</a>
                            <a class="dropdown-item" href="{{path('app_registration')}}">Inscription</a>
                            {% endif %}
                        </div>
                    </li>
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                            <a class="dropdown-item text-light" href="{{path('app_basket_display')}}"><i class="fa-solid fa-basket-shopping"></i></a>
                    </li>
                    {% endif %}
                </ul>
            </div>
            </nav>
        
        </header>
        
        {% for type, messages in app.flashes %}
            {% for message in messages %}
            <div>
                {{ message }}
            </div>
            {% endfor %}
        {% endfor %}
      
        {% block body %}{% endblock %}
       
        
        {% block javascripts %}
        {% endblock %}
        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
        
        
    </body>
</html>
